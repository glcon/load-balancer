apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # This URL points to your prometheus container in the docker-compose network
    url: http://prometheus:9090
    isDefault: true
    editable: true